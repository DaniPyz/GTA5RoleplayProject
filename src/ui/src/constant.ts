import { Hud, View } from 'index';
import { Temp, Autosalon } from 'views';

export const DEFAULT_VIEW: View = 'Autosalon';
export const DEFAULT_HUD_LIST: Hud[] = ['Temp'];

export const HUD_LIST = {
	Temp
};

export const VIEW_LIST = {
	Temp,
	Autosalon
};
